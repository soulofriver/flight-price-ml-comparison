# flight-price-ml-comparison
Machine Learning pipeline for predicting flight ticket prices using XGBoost and Random Forest. Includes data preprocessing, one‑hot encoding, log‑target regression, model evaluation (MAE, RMSE, R²), and visual comparison of predictions.
